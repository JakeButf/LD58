using UnityEngine;

public class GameState : MonoBehaviour
{
    void Awake()
    {
        PlayerInput.Initialize();
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
